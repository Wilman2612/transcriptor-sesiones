# Entorno y secretos
.env
.venv/
venv/

# Python (solo artefactos de build en la raíz; NO web/dist, que sí se versiona)
__pycache__/
*.pyc
*.pyo
*.egg-info/
/dist/
/build/
.pytest_cache/
.ruff_cache/

# Datos de la app (audio, chunks, base de datos, exports) — pesados y/o con PII
data/uploads/
data/chunks/
data/exports/
data/*.db

# Muestras: audio real de sesiones + transcripcion con nombres reales (PII). No subir.
sample/

# Artefactos de revision / herramientas (regenerables)
screenshots/
.playwright-cli/
/*.png

# Log del instalador (se genera en la carpeta del programa)
instalacion.log
